log-level="debug"

{{if .WithDatabase}}
[database]
scheme = "postgres"
user = "postgres"
host = "localhost"
port = 5435
name = "mmw"
sslmode = "disable"
{{end}}

{{if .WithConnect}}
[server]
port = 8090
scheme = "https"
host = "localhost"
allowed-origins = ["*"] # IMPORTANT; Change this in production !

[auth-server]
port = 8091
scheme = "http"
host = "localhost"
{{end}}
