-- +goose Up
-- +goose StatementBegin


SELECT 'Empty initial up migration';

-- +goose StatementEnd

-- +goose Down
-- +goose StatementBegin

SELECT 'Empty initial down migration';

-- +goose StatementEnd
